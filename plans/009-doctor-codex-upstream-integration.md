# Doctor Codex integration

User-approved scope: exact App Server schema/adapter coverage, immutable upstream source context, and guarded SDK execution with CLI fallback selected before dispatch. Existing Doctor owns this revision; no new tweak. Lead owns integration and acceptance; SDK and upstream helpers own disjoint modules.

Acceptance: semantic schema changes and unknowns; real production adapter routing/correlation with synthetic inputs; isolated safe backend reads; provenance/cache substitution/offline handling; SDK capability denial and no duplicate paid execution; existing focused tests, typecheck, build, full suite, catalog check; guarded Tweakers-only final promotion and visible report check. Native Codex restart and releases are excluded.

Work status: implementation and independent review complete. Existing dirty work preserved. Assignment contracts are under /tmp/tweakers-updater-agents. Durable verification logs and candidate evidence are under ~/Library/Application Support/Tweakers/doctor/implementation-acceptance/20260915-codex-updater.

Verification: typecheck, full build, installer packaging, catalog and tweak synchronization checks passed. Full package suite: 2,607 passed, 12 skipped, zero failed; script suites: 1 and 31 passed; tweaks: 412 passed; MCP lifecycle: 85 baseline tests validated. Final focused Doctor/account-router checks: 112 passed. A real isolated backend answered initialization, model listing, unauthenticated account read and empty thread reads. Its generated schema bundle resolved every inventoried interface, and packaged production adapter checks passed.

Live delivery: signed maintenance candidate prepared. The first promotion attempt stopped before cutover because the manager retained official build 8881 while the installed official app was 9275. Revalidated the official app with production trust checks and atomically refreshed identity evidence under the lifecycle lock, preserving selection. The next attempt reached Doctor, which reported authentication_binding_invalid and required a user account reconnect. No credentials were refreshed. Runtime readiness could not pass. Journal-backed recovery completed after closing the exact Doctor helper; the restored app matches the pre-maintenance SHA-256 fingerprint and the official app process stayed unchanged. The new report section cannot be claimed live-accepted from the older saved report. Native Codex remains untouched; no release was published.

Known capability boundary: SDK 0.154.0 lacks required execution safeguards, so its production registry remains closed. Authenticated refresh/approval/turn behavior remains unresolved when no behavior evidence exists; schema checks and synthetic routing never grant that proof.

Retention: four exact task-owned failed staging folders (4,671,644 allocated KiB) moved to Trash under lifecycle/promotion locks; Trash was not emptied. The reusable signed candidate is retained for delivery after reconnect, together with the original installation and its one rollback. No historical archive was needed for these reproducible staging copies. Free disk measured 57,572,663,296 bytes before cleanup and 57,572,618,240 after; Trash moves do not reclaim disk space. Manifest and result are retained beside the candidate.
